require(sGPfit)
require(aghq)
require(TMB)
require(tidyverse)
require(Matrix)
require(lubridate)
require(OSplines)
require(ISOweek)
source(file = "function.R")
compile(file = "mixed.cpp")
dyn.load(dynlib("mixed"))
load(file = "final.data")
